/*
 * Icon Atom Component
 *
 */

import React from "react"
// import PropTypes from "prop-types"

const Icon = (props) => {
  return <>Icon</>
}

// Icon.defaultProps = {
// text: "",
// modifier: "",
// }

// Icon.propTypes = {
// text: PropTypes.string.isRequired,
// modifier: PropTypes.oneOf(["", "--something"]),
// }

export default Icon
